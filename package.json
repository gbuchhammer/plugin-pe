{
  "name": "@buchorg/plugin-pe",
  "version": "1.0.0",
  "description": "Country shell — Perú — YOpen Framework",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],

  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },

  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --external react,react-dom",
    "prepublishOnly": "npm run build"
  },

  "dependencies": {
    "@buchorg/plugin-sdk": "^1.0.0",
    "@buchorg/ui-core":    "^1.0.0"
  },
  "peerDependencies": {
    "react":     ">=18",
    "react-dom": ">=18"
  },
  "devDependencies": {
    "tsup":       "^8.0.0",
    "typescript": "^5.4.0",
    "react":      "^18.3.0",
    "@types/react": "^18"
  }
}
